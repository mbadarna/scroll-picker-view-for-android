・TouchScroller.moveToIndex
ループ指定のときもっとも近い方向に移動するように。

・リスナ対応
ScrollPickerViewListener対応

・デザインの調整
ラベルに影を追加
透過デザインのレイヤ変更

・日時対応
月によって日のレンジをへんこうする

・ScrollPickerView,SlotView
ラベルのAlignを指定できるように
ラベルの色を指定できるように
ラベルの影を指定できるように

・SlotView,TouchScroller
動的にラベルを増減できるように

・TouchScroller
イベント量の調整
サイズによってスクロールスピードの調整

・ScrollPickerView
タッチスクロールを受け付けない指定の追加

・表示
枠内に表示する最大件数を3～10の間で設定できるようにする。

・AbstractSlotView
AbstractSlotViewを公開クラスにして、継承使用できるようにする。
カスタマイズすれば、画像も使用できるようにする。

・フォーカス色
